 import {Tool} from '../../service/common-tool.js';
 export default {
     name: 'user-card',
     methods: {
         jump() {
             //    alert(111);
             var tool = new Tool();
           
             tool.alert('asfsaf');
         }
     }
 }
